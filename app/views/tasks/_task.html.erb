<%= content_tag_for :li, task do %>
  <input type="checkbox" <%= 'checked="checked"' if task.completed %> data-url="<%= h toggle_task_path(task) %>" data-remote="true" data-method="post" />
  <%= task.name %>
<% end %>
